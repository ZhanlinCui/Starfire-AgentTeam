# Secrets for this workspace (gitignored). Copy to .env and fill in real values.
# GOOGLE_API_KEY is inherited from the parent .env — set per-agent only if
# this agent needs a different key (e.g. hitting a different project quota).
