name: starfire-dev
version: 1.0.0
description: Starfire codebase conventions, past bugs, quality standards, and coordination workflows
author: Starfire
tags: [conventions, quality, coordination, codebase]

# Per-runtime adaptors live under adapters/<runtime>.py and are resolved via
# the platform's plugin registry. Listed here so the canvas can filter
# install options by workspace runtime.
runtimes:
  - claude_code
  - deepagents

skills:
  - review-loop
