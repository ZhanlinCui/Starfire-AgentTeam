name: AutoGen Agent
description: Microsoft AutoGen — conversable agent with tool use and multi-agent orchestration
version: 1.0.0
tier: 2

runtime: autogen
model: openai:gpt-4.1-mini

env:
  required:
    - OPENAI_API_KEY
