name: PM
description: Agent Molecule Project Manager — coordinates Marketing, Research, Dev teams
version: 1.0.0
tier: 2

runtime: claude-code
runtime_config:
  model: sonnet
  auth_token_file: .auth-token
  timeout: 0
