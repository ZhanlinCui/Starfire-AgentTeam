name: Security Auditor
description: Security auditing, vulnerability assessment, and compliance review
version: 1.0.0
tier: 2

runtime: claude-code
runtime_config:
  model: sonnet
  auth_token_file: .auth-token
  timeout: 0
