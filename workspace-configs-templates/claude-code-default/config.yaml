name: Claude Code Agent
description: General-purpose Claude Code workspace
version: 1.0.0
tier: 2

runtime: claude-code
runtime_config:
  model: sonnet
  required_env:
    - CLAUDE_CODE_OAUTH_TOKEN
  timeout: 0
